# Cascade — Vercel Python runtime dependencies.
# Vercel reads this file when deploying Python Serverless Functions.
# Keep it in sync with pyproject.toml [project.dependencies]; this is the
# production-only subset (no dev tools).

fastapi>=0.115.0
mangum>=0.17.0

# AWS data layer
asyncpg>=0.29.0
pgvector>=0.3.0
aioboto3>=13.0.0
boto3>=1.34.0

# Models + serialization
pydantic>=2.8.0
pydantic-settings>=2.4.0
orjson>=3.10.0

# LLM + embeddings
google-genai>=0.3.0
voyageai>=0.3.0

# HTTP + streaming + retries
httpx>=0.27.0
sse-starlette>=2.1.0
tenacity>=9.0.0
websockets>=13.0

# Data-source clients used by workers
feedparser>=6.0.11
beautifulsoup4>=4.12.0
yfinance>=0.2.40
praw>=7.7.1
pypdf>=5.0.0

# Local-only env loading (no-op on Vercel where env vars are platform-injected)
python-dotenv>=1.0.1
